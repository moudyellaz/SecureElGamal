# --------------------------------------------------------------------
true : debug
true : warn_Z, warn_Y
true : -traverse
true : bin_annot

# --------------------------------------------------------------------
<src>   : include
<src/*> : package(zarith)
